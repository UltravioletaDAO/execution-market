# Dependencies
node_modules/
.venv/
__pycache__/
*.py[cod]
*$py.class

# Build outputs
dist/
build/
out/
*.egg-info/
.eggs/

# Environment files
.env
.env.local
.env.*.local
.env.cloud
.env.production
.env.staging
.secrets/

# Production env files (component-specific)
dashboard/.env.production
dashboard/.env.production.local
mcp_server/.env
contracts/.env

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage
coverage/
.coverage
htmlcov/
.pytest_cache/

# Terraform
*.tfstate
*.tfstate.*
.terraform/
*.tfvars
!*.tfvars.example
*.tfplan
infrastructure/terraform/tfplan
infrastructure/terraform/lambda/*.zip

# Video project artifacts
video-v*/node_modules/
video-v*/out/
video/node_modules/
video/out/
videos/*/node_modules/

# Admin dashboard
admin-dashboard/node_modules/

# Vendor contracts (use forge install / submodules)
contracts/lib/

# Supabase temp
supabase/.temp/

# Large video assets (manage separately)
videos/insights-2026/

# Agent tool symlink dirs (only contain symlinks, break on Windows git)
.codex/
.opencode/
# Symlinks in .claude/skills that point to .agents/ (content tracked there)
.claude/skills/vercel-react-best-practices
.claude/skills/web-design-guidelines
.claude/skills/skill-creator

# Terra4mice state (local convergence tracking)
terra4mice.state.json
terra4mice.state.json.bak
mcp_server/terra4mice.state.json

# Windows artifacts
nul

# ECS task definition artifacts (generated during deployment)
*_td*.json
.deploy_tag

# Auto-generated agent instruction files
AGENTS.md

# ===========================================
# Temp files from coding sessions
# ===========================================
tmp_*
C:tmp*

# ===========================================
# IaC state files (generic glob)
# ===========================================
*.state.json.bak

# Claude local overrides (contains real AWS account IDs, wallet addresses)
CLAUDE.md.local

# Agent memory (local per-machine)
.claude/agent-memory/

# Claude session files (local per-machine)
.claude/.irc-nick
.claude/teams/
.claude/tasks/

# Script output artifacts
scripts/output.txt
scripts/server_pid.txt
mcp_server/server_pid.txt
scripts/kk/report-erc8004-registration-*.json
scripts/kk/report-distribute-*.json
scripts/kk/report-bridge-*.json
scripts/kk/report-sweep-*.json

# Misc
.unused/
.internal/

# CI debug artifacts (downloaded from GitHub Actions)
ci-error.txt
ci-output.txt
ci-logs.zip

# Test coverage artifacts
mcp_server/coverage.xml

# IRC nick state (per-machine)
.claude/skills/irc-agent/.claude/.irc-nick

# KK agent workspace data (generated)
scripts/kk/data/workspaces/

# Private keys and secrets (NEVER commit)
*.keys.json
*private*.json
*secret*.json
scripts/wallet-management/.keys.json

# Internal docs (private business correspondence)
docs/internal/

# Articles (managed separately, not part of public repo)
docs/articles/

# Planning docs (internal operational notes, roadmaps, handoffs)
docs/planning/

# Tweet screenshots and media
docs/tweets/

# Chat logs
docs/chats/

# Reports (contain operational wallet details and internal audit data)
docs/reports/

# Obsidian vault (internal knowledge graph)
vault/

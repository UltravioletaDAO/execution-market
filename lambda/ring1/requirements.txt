# Ring 1 PHOTINT Verification Lambda Dependencies
# Versions pinned to match mcp_server/requirements.txt

# Image processing (EXIF extraction, perceptual hashing)
Pillow>=10.2.0
imagehash>=4.3.0
piexif>=1.1.3

# HTTP client (AI provider API calls, Supabase REST)
httpx>=0.26.0

# Blockchain (wallet address validation, on-chain checks)
web3>=6.15.0

# AWS SDK (Secrets Manager, S3 evidence download)
boto3>=1.34.0

# Database (Supabase client for status updates)
supabase>=2.0.0

# AI providers (multi-model verification)
google-generativeai>=0.8.0
anthropic>=0.30.0
openai>=1.0.0

# Utils
python-dateutil>=2.8.0

# City-as-a-Service — AAS System Integration Decision Packet (2026-05-27 03:00)

> Scope: Execution Market AAS / City-as-a-Service internal planning only
> Governing priority: `~/clawd/DREAM-PRIORITIES.md`
> Status: conservative system-integration planning packet; no customer/public route, dispatch, pricing, payment re-verification, live Acontext readiness, ERC-8004 reputation, worker Skill DNA, exact GPS/raw metadata exposure, or worker-copyable doctrine

## 1. Priority resolution

`DREAM-PRIORITIES.md` was read first and controls this dream session. It explicitly stops AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 during dreams.

The 03:00 cron payload still contained stale AutoJob, Frontier Academy, and KK v2 instructions, including an AutoJob-first pull. Those instructions were intentionally not executed. This packet stays inside Execution Market AAS / City-as-a-Service and uses the system-integration portions of the prompt only where they reinforce the active AAS focus.

## 2. Source artifacts consumed

This packet synthesizes existing internal planning and proof artifacts without claiming new runtime readiness:

- `CITY_AS_A_SERVICE_Acontext_MEMORY_BRIDGE.md`
- `CITY_AS_A_SERVICE_DECISION_SUPPORT_CONTROL_PLANE.md`
- `CITY_AS_A_SERVICE_OBSERVABILITY_AND_SUCCESS_METRICS.md`
- `CITY_AS_A_SERVICE_COORDINATION_CARRY_FORWARD_MATRIX.md`
- `CITY_AS_A_SERVICE_AAS_SYSTEM_INTEGRATION_FLYWHEEL_IMPLEMENTATION.md`
- `CITY_AS_A_SERVICE_AAS_STRENGTH_CONNECTION_CONTROL_PACKET_IMPLEMENTATION.md`
- `CITY_AS_A_SERVICE_6AM_FINAL_WRAP_2026_05_25.md`

The recurring pattern across those files is now clear:

```text
reviewed AAS artifact
-> compact decision object
-> four-ID handoff header
-> memory/Acontext candidate
-> operator decision surface
-> observability row
-> next-proof slot
```

The product advantage is not any one artifact. It is keeping that chain intact without promoting blocked claims.

## 3. Current strength connection map

| Current strength | Safe internal use | Verification badge in this packet | Must not be inferred |
|---|---|---|---|
| Latest `city_ops` proof graph and AAS family work | Use the landed fixture/read-surface/schema-gate artifacts as the implementation substrate for the next internal packet | consumed from local docs and prior proof descriptions, not rerun here | no claim that every code test was rerun by this packet |
| 8/8 chain payment integration | Treat as production-confidence context when choosing seams that could later become payable tasks | declared context only | no payment coverage reverified, no escrow/release guarantee, no pricing/customer quote |
| Intelligent memory / reviewed insight work | Convert reviewed AAS outcomes into Acontext candidate objects after review | planning-ready, live sink not proven | no `acontext_sink_ready`, no runtime parity, no raw transcript authority |
| Production infrastructure operational | Prefer operator/admin surfaces that could become deployable later | declared context only | no production health reverified, no public route readiness |
| Legendary agent coordination | Preserve invariant IDs, sticky blocked claims, and one-next-proof discipline in every handoff | consumed as operating discipline from existing control-plane docs | no autonomous routing, no public reputation score, no worker Skill DNA |

## 4. Four integration lanes

### 4.1 Memory system ↔ Acontext lane

**Goal:** every reviewed AAS package should become a retrieval-ready candidate only after review.

Minimum object flow:

```text
internal_package_record
-> operator_read_surface or customer_output_schema_gate
-> compact_decision_object
-> aas_memory_candidate
-> live Acontext write/retrieve parity attempt
-> retrieval_result_with_claim_boundaries
```

Required fields for the candidate:

- `proof_anchor_id`
- `coordination_session_id`
- `compact_decision_id`
- `review_packet_id`
- `aas_family_id`
- `boundary_label`
- `safe_to_claim[]`
- `do_not_claim_yet[]`
- `selected_behavior_change`
- `next_smallest_proof[]`
- `raw_metadata_release_allowed=false`
- `worker_copyable_doctrine_ready=false`

Current posture:

```text
acontext_candidate_shape_ready=true
acontext_sink_ready=false
runtime_parity_proven=false
raw_transcript_authority=false
```

Smallest next proof if runtime prerequisites clear:

```text
Write one reviewed, non-site-specific AAS memory candidate into local Acontext.
Retrieve it by family + boundary + proof_anchor_id.
Verify safe_to_claim[] and do_not_claim_yet[] survive unchanged.
Do not run a second parity attempt in the same proof unless the first attempt explicitly passes and a new gate authorizes expansion.
```

### 4.2 IRC/session management lane

**Goal:** live coordination should carry compact state, not raw transcript dependency.

Every IRC/admin handoff should start with this four-ID header:

```text
proof_anchor_id: <proof block or package id>
coordination_session_id: <session id>
compact_decision_id: <decision object id>
review_packet_id: <review packet id>
```

Then it should include:

1. one current safe claim;
2. the sticky blocked-claim footer;
3. the declared-vs-verified strength badges;
4. exactly one next-proof slot;
5. the current stop condition.

Session-management enhancement should be judged by field survival, not verbosity. A good handoff is one that a later agent can resume without replaying the full transcript and without accidentally converting an approval request into approval.

Current posture:

```text
irc_session_header_pattern_ready=true
irc_runtime_coordination_mutated=false
session_rebuild_ready=false
raw_transcript_replay_required=false_for_normal_path
```

Smallest next proof:

```text
Create a read-only session handoff capsule for one AAS family that preserves the four IDs, claim boundaries, selected next proof, and stop condition across pickup/telemetry/export surfaces.
```

### 4.3 Cross-project decision-support lane

**Goal:** adjacent AAS families should become comparable without becoming launch doctrine.

The decision-support control plane should choose between families by answering:

- Which family has the narrowest next proof?
- Which family has the fewest authority hazards?
- Which family has the clearest blocked claims?
- Which family has a reviewed output shape but no customer/public exposure?
- Which family improves the reusable AAS template most?

Current recommended comparison set:

| Family | Current useful role | Best next internal step |
|---|---|---|
| Retail Reality | approval-boundary template | hold/approval decision or cross-family matrix row |
| Local Data Collection | method-bounded micro-observation template | synthetic internal/admin sample output, then separate hold decision |
| Document / Handoff | logistics handoff boundary | continue approval-state clarity only |
| Incident Verification | incident package validation pattern | keep validator/read-surface claims separate from approval |
| Compliance Desk | single approved package-label boundary | do not inflate label approval into delivery authorization |

Current posture:

```text
cross_family_comparison_ready=true
cross_project_autorouting_ready=false
autonomous_prioritization_ready=false
customer_catalog_choice_ready=false
```

Smallest next proof:

```text
Add one cross-family decision row that compares Retail Reality and Local Data Collection using only internal/admin fields: boundary type, evidence maturity, blocked authority class, next proof, and launch-readiness=false.
```

### 4.4 Agent observability and success metrics lane

**Goal:** measure whether coordination and memory improve future AAS decisions.

The first live-ish success metric should not be generic agent throughput. It should be claim-boundary and carry-forward survival.

Recommended metric additions:

- `aas_handoff_four_id_completeness_rate`
- `aas_claim_boundary_survival_rate`
- `aas_declared_vs_verified_badge_survival_rate`
- `aas_one_next_proof_slot_survival_rate`
- `aas_blocked_claim_reappearance_count`
- `aas_memory_candidate_retrieval_parity_rate`
- `aas_family_decision_row_reuse_rate`

A successful agent coordination loop is one where later agents:

1. keep the same four IDs;
2. carry safe and blocked claims together;
3. do not repeat declared payment/infra context as current proof;
4. choose the same next proof unless a new artifact justifies changing it;
5. stop when a blocked claim would be promoted.

Current posture:

```text
agent_success_metric_shape_ready=true
agent_observability_live_dashboard_ready=false
erc8004_reputation_ready=false
agent_success_score_reputation_ready=false
```

Smallest next proof:

```text
Emit a deterministic internal/admin scorecard over one proof block showing whether four-ID completeness, claim-boundary survival, declared-vs-verified badges, and one-next-proof discipline survived across the latest AAS handoff chain.
```

## 5. Recommended next implementation slice

The best next slice is not another broad planning document. It is one narrow internal/admin proof object:

```text
AAS carry-forward success scorecard
```

Inputs:

- one proof anchor from the existing AAS fixture graph;
- one family row from Retail Reality or Local Data Collection;
- the latest compact decision / package / read-surface artifacts available for that family;
- existing claim boundaries.

Outputs:

- `aas_carry_forward_success_scorecard.json`
- one loader/validator that refuses missing four-ID fields;
- one fixture equality test;
- one test that fails if any blocked claim appears in `safe_to_claim[]`;
- one test that fails if more than one next-proof slot appears;
- one implementation note documenting no live Acontext, dispatch, public route, payment, production, reputation, GPS/raw metadata, or worker-doctrine readiness.

Why this is the right next step:

1. It directly connects memory/Acontext planning, IRC handoff discipline, decision support, and observability.
2. It amplifies the current coordination strength instead of opening a new product surface.
3. It creates a reusable gate for every future AAS family.
4. It avoids the stopped AutoJob/Frontier/KK tracks.
5. It can be implemented and tested without Docker or live Acontext prerequisites.

## 6. Blocked claims that remain sticky

Do not infer any of the following from this packet:

```text
customer_copy_ready
customer_delivery_approved
publication_approved
public_catalog_ready
public_route_ready
controlled_pilot_ready
front_door_sku_ready
public_pricing_or_customer_quote_ready
operator_queue_launch_ready
autonomous_dispatch_ready
cross_project_autorouting_ready
autonomous_prioritization_ready
agent_observability_live_dashboard_ready
erc8004_reputation_ready
agent_success_score_reputation_ready
worker_skill_dna_ready
live_acontext_runtime_parity
acontext_sink_ready
payment_or_production_reverified
exact_gps_or_raw_metadata_release_allowed
private_operator_context_release_allowed
raw_transcript_authority
worker_copyable_aas_doctrine
worker_copyable_data_collection_doctrine
```

## 7. Morning handoff

Use this packet as the 03:00 system-integration bridge. The safest next build is an internal/admin AAS carry-forward success scorecard. It should make the coordination excellence measurable by proving that the same four IDs, safe claims, blocked claims, declared-vs-verified badges, and one next-proof slot survive across AAS handoffs.

If live Acontext prerequisites are repaired later, run exactly one write/retrieve parity attempt against a reviewed, non-site-specific AAS memory candidate. Until then, keep Acontext as candidate shape and planning lane only.

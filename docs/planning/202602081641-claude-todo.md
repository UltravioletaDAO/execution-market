# Pendientes — Execution Market MVP

> 2026-02-08 16:41 | Post-fix escrow addresses + E2E test suite
> Actualizado: 2026-02-08 ~19:00 | Balances re-verificados on-chain, Avalanche $103

---

## Blockers (impiden testing real en otras redes)

| # | Qué | Impacto |
|---|-----|---------|
| ~~B1~~ | ~~Production wallet sin USDC en otras redes~~ | **RESUELTO** — wallet `0xD386...` fondeada: $111.92 total (7 pares con saldo). Solo Ethereum = $0. |
| ~~B3~~ | ~~Reputation queries fallan en TODAS las redes~~ | **RESUELTO** — facilitator v1.29.8 deployed. Base: score=100, count=1. Las 7 redes responden OK. |

---

## Bugs Abiertos (menores)

| # | Qué | Severidad |
|---|-----|-----------|
| **L2** | `GET /escrow/config` devuelve la dirección legacy `0xC409` en vez de la nueva `0xb948...Eb4f` | Baja |
| **L3** | `reputation.py` no valida que `score` sea 0-100 antes de llamar al facilitator | Baja |
| **$0.10** | $0.10 USDC atascados en el vault (tx `0xda31cbe...`) — necesita refund o expiración | Info |

**Nota:** `total-supply` endpoint ahora devuelve error amigable (`"totalSupply() is not available on the current contract implementation"`) en vez de revert crudo. No es bug nuestro — el Identity Registry no tiene ERC721Enumerable.

---

## Test Coverage (54% → meta 80%+)

29 test cases faltan de los 73 planeados. Los más importantes:

### P0 — Core flow gaps

- F2/F3/F4: Verificar que el worker recibe USDC, treasury recibe fee, auth se consume
- D1/D3: Verificar que el EIP-3009 auth se almacena correctamente

### P1 — Edge cases

- C4/C5/C6: Bounty mínimo/máximo, deadline expirado
- G2/G3: Cancel después de accept/submission
- H1/H2: Rating completo (requiere task completado con pago real)

---

## 7 GAPs — Decisiones de Diseño (necesitan brainstorming)

| GAP | Pregunta | Estado Actual |
|-----|----------|---------------|
| **GAP-1** | ¿ERC-8004 registration debería ser **hard gate** para crear tasks? | Soft gate (siempre pasa) |
| **GAP-2** | ¿Auto-expiración de tasks con **pg_cron**? | Check-on-read (no hay trigger) |
| **GAP-3** | ¿Se puede cancelar un task **después de submission**? | No se puede |
| **GAP-4** | ¿Se puede dejar reputation en tasks **fallidos/cancelados**? | Solo en completed |
| **GAP-5** | ¿Settlement multichain es **real** o solo label? | Solo label (siempre Base) |
| **GAP-6** | ¿Workers necesitan **registro on-chain** ERC-8004? | No requiere |
| **GAP-7** | ¿Task creation debería ser **idempotente** (retry-safe)? | No (cada POST = nuevo task) |

---

## Infra/Deploy Pendiente

- Múltiples commits con fixes de escrow, token registry y wallet **no están deployed** todavía
- Se actualizó la private key en AWS Secrets (`em/x402`) — ECS necesita **force re-deploy** para usar la nueva key
- Después de push + deploy, el MCP server usará las direcciones y wallet correctas

---

## Balances Actuales (wallet `0xD386...`)

| Red | USDC | EURC | AUSD | USDT0 | PYUSD |
|-----|------|------|------|-------|-------|
| Base | **$5.48** | $0 | — | — | — |
| Ethereum | $0 | $0 | $0 | — | $0 |
| Polygon | **$1.80** | — | $0 | — | — |
| Arbitrum | **$0.60** | — | $0 | $0 | — |
| Celo | **$0.60** | — | — | $0 | — |
| Monad | **$0.40** | — | $0 | — | — |
| Avalanche | **$92.93** | **$10.12** | $0 | — | — |

**Total: $111.92** (7 pares con saldo, 11 en $0). Avalanche = 92% del total.

---

## Próximos Pasos Sugeridos

1. **Push + deploy** para que todos los fixes lleguen a producción
2. **Force re-deploy ECS** para que use la nueva wallet key
3. **Fondear stables opcionales** — AUSD en 5 redes, EURC en Base/Ethereum, USDT0 en Arbitrum/Celo, PYUSD en Ethereum
4. **Brainstorming de GAPs** — definir los 7 puntos de diseño
5. **Completar tests P0** (F2-F4, D1, D3)

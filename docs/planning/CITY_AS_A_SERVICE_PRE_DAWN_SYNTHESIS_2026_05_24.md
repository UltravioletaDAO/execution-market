# City-as-a-Service — Pre-Dawn Synthesis (2026-05-24)

## Governing priority

`~/clawd/DREAM-PRIORITIES.md` was read first and controlled the session. It explicitly stops AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 during dreams.

The stale cron payload requested AutoJob pull/analysis, Frontier Academy expansion, and KK v2 work. Those tracks were not pulled, analyzed, edited, or expanded. Work stayed inside Execution Market AAS / City-as-a-Service.

## Night pattern

Tonight advanced the Retail Reality lane from a read-only internal package into an auditable, still-held customer-exposure decision point:

```text
Retail Reality operator read surface
-> customer-output schema gate
-> synthetic internal/admin sample output
-> explicit sample-output hold decision
-> pending human-operator approval request
```

The strategic shift is important: the system can now present an exact candidate boundary to a human without pretending that the candidate has been approved. That is the correct seam between autonomous preparation and human publication authority.

No part of tonight's work creates customer copy, delivery authorization, publication readiness, public routes, catalog readiness, pricing, queue launch, dispatch, ERC-8004 reputation, worker Skill DNA, runtime parity, payment/production reverification, exact GPS/raw metadata release, retail authority, or worker-copyable doctrine.

## What connected tonight

### 1. Retail Reality became a reusable approval-boundary ladder

The new Retail Reality artifacts prove a narrow product grammar:

```text
schema allowed fields
+ one synthetic sample
+ explicit hold decision
+ pending approval request
+ digest-bound selected text boundary
+ redaction / authority / delivery-path checks still false
```

This is stronger than merely drafting customer copy. The artifact chain records exactly what could be reviewed later while preserving the fact that no review approval has happened.

The safe claim is only:

```text
retail_reality_human_operator_approval_request_landed
```

It inherits the earlier safe internal claims:

```text
retail_reality_operator_read_surface_landed
retail_reality_customer_output_schema_gate_landed
retail_reality_internal_sample_output_landed
retail_reality_sample_output_review_decision_landed
```

### 2. The hold/approval boundary is now explicit enough for daytime choice

The latest artifact has:

```text
approval_request_status = pending_human_operator_review_not_approved
authorized_delivery_path.path = none_until_separate_human_operator_approval_record
```

That means daytime can make a clean fork:

- if Saúl wants customer exposure, create a separate human-operator approval record over the exact selected text;
- if not, leave the request pending and keep building internal packaging or runtime parity prerequisites;
- do not infer approval from the existence of a request.

This pattern should generalize to other AAS lanes: autonomous systems can prepare the candidate boundary, but approval must be a separate artifact with its own digest, redactions, delivery path, and blocked-claim list.

### 3. Blocked claims are becoming product infrastructure, not friction

Retail Reality now carries an intentionally long blocked-claim surface next to every safe claim. That prevents the most likely failure mode for AAS packaging: treating a polished internal/admin sample as a public offer, pilot, catalog row, worker instruction, or reputation-producing event.

The night reinforced this doctrine:

```text
candidate boundary != approval
approval request != customer delivery
sample output != public copy
operator read surface != catalog route
internal package != dispatchable task
```

## Current safe claims from tonight

```text
retail_reality_customer_output_schema_gate_landed
retail_reality_internal_sample_output_landed
retail_reality_sample_output_review_decision_landed
retail_reality_human_operator_approval_request_landed
```

These claims are internal/admin-only or approval-request-only. They do not promote customer/public/runtime/reputation/payment/location/legal/retail-authority readiness.

## Still blocked

Do not infer any of the following from tonight's work:

```text
human_operator_approval_recorded
customer_copy_ready
customer_delivery_approved
publication_approved
public_catalog_ready
public_route_ready
controlled_pilot_ready
front_door_sku_ready
public_pricing_or_customer_quote_ready
operator_queue_launch_ready
autonomous_dispatch_ready
erc8004_reputation_ready
worker_skill_dna_ready
live_acontext_runtime_parity
acontext_sink_ready
payment_or_production_reverified
exact_gps_or_raw_metadata_release_allowed
private_retail_context_release_allowed
permanent_business_status_claim
inventory_guarantee
brand_compliance_certification
employee_performance_judgment
consumer_safety_certification
continuous_availability_monitoring
retail_authority_claims
worker_copyable_retail_doctrine
```

## Daytime recommendations

### 1. Best product move

If Saúl wants to review Retail Reality customer exposure, create a separate human-operator approval record over the pending request.

Required gate:

- consume only `retail_reality_human_operator_approval_request.json`;
- name the exact selected text boundary and verify its digest;
- record passed/failing redaction checks;
- record a human-selected delivery path or keep it absent;
- keep public/catalog/pilot/dispatch/reputation/runtime/location/retail-authority/worker-doctrine claims blocked unless explicitly approved by the new artifact;
- do not mutate the pending request into an approval.

### 2. Best conservative move

If customer exposure is not a daytime priority, leave the request pending and create a small internal operator queue/status card that lists pending AAS approval requests without adding approval semantics.

Required gate:

- internal/admin only;
- read-only;
- pass-through over request artifacts;
- safe/blocked claims visible together;
- no public route, no customer copy, no delivery, no dispatch.

### 3. Best runtime move

Acontext remains a separate blocker. Do not mix Retail Reality approval work with runtime-memory claims until Docker/socket/API/dashboard prerequisites are repaired and a fresh preflight explicitly allows one live write/retrieve parity pass.

### 4. Best strategic posture

Retail Reality is now the low-authority template for **approval-boundary AAS**. The repeatable recipe is:

```text
reviewed internal package
-> operator surface
-> output schema gate
-> internal sample
-> hold decision
-> pending human approval request
-> separate approval record only if a human explicitly approves
```

That recipe can support adjacent AAS packages without turning draft copy into public product readiness.

## Morning handoff

Daytime should choose exactly one narrow move:

1. **Approval record:** create a separate Retail Reality human-operator approval record over the pending request, only if Saúl wants customer exposure.
2. **Pending-queue surface:** build a read-only internal/admin pending-approval queue card over the request artifact.
3. **Runtime parity:** repair Docker/Acontext prerequisites before any live memory/runtime claims.

AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 remain stopped for dream work by `DREAM-PRIORITIES.md`.

# City-as-a-Service — Pre-Dawn Synthesis (2026-05-27)

## Governing priority

`~/clawd/DREAM-PRIORITIES.md` was read first and controlled the session. It explicitly stops AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 during dreams.

The cron payload still carried older requests to pull/analyze AutoJob, expand Frontier Academy, and continue KK v2. Those tracks were not pulled, analyzed, edited, expanded, or tested. Work stayed inside Execution Market AAS / City-as-a-Service.

## Repo state

- Repo: `projects/execution-market`
- Branch: `feat/operator-route-regret-panel`
- Sync check: `git pull --ff-only` returned `Already up to date`
- Pre-existing untracked file left untouched: `scripts/sign_req.mjs`
- Latest pushed implementation before this synthesis: `a61db4d3` (`Add AAS exponential value pathfinder`)

## Night pattern

Tonight moved the AAS portfolio from individual package ladders into a connected internal/admin coordination stack:

```text
AAS portfolio promotion ledger
-> AAS portfolio next-gate board
-> AAS portfolio operator authorization packet
-> AAS system integration / strength connection stack
-> AAS exponential value pathfinder
-> AAS pre-dawn synthesis handoff
```

The important product shift is not “launch readiness.” It is that the system now has a clean way to convert many tempting strategy signals into one bounded next-proof queue while preserving explicit blocked claims.

No part of tonight's work creates customer copy, delivery authorization, publication readiness, public/catalog routes, pricing or quotes, queue launch, dispatch, ERC-8004 reputation, Worker Skill DNA, live Acontext/runtime parity, payment/production reverification, exact GPS/raw metadata exposure, private context release, or worker-copyable doctrine.

## What connected tonight

### 1. Portfolio status became one operator decision board

The promotion ledger and next-gate board made the five active AAS families comparable without promoting any of them:

```text
Retail Reality
Compliance Desk
Document / Handoff
Incident Verification
Local Data Collection
```

Retail Reality remains the cleanest future customer-exposure candidate only because it already has a selected boundary pending human review. Compliance Desk is second only if a specific delivery/publication path is explicitly authorized. The other lanes stay internal prerequisite-only.

### 2. Authorization is now separated from synthesis

`aas_portfolio_operator_authorization_packet.json` prepares two future questions but records no answer. This is a useful safety seam: daytime can ask or answer exactly one question later without letting a night synthesis become an approval record.

### 3. Exponential value is now mapped, not claimed

`aas_exponential_value_pathfinder.json` maps five compounding loops:

```text
memory insight -> Acontext proof
IRC four-ID handoff -> swarm coordination
cross-project signal -> AAS gate selection
agent observability -> private selection
landed code -> proof ladder
```

The highest multiplier next proof remains:

```text
acontext_runtime_memory_prerequisites_then_single_live_parity_attempt
```

But that is only a recommended internal/admin proof slot. It is not live Acontext readiness, not runtime parity, and not permission to write or retrieve live memory.

### 4. The 5 AM handoff makes the priority firewall explicit

`aas_pre_dawn_synthesis_handoff.json` now records that AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 were skipped because `DREAM-PRIORITIES.md` wins over stale cron payloads. This reduces the chance that daytime or the next dream accidentally reopens stopped tracks.

## Current safe claims from tonight

```text
admin_aas_portfolio_promotion_ledger_landed
admin_aas_portfolio_next_gate_board_landed
admin_aas_portfolio_operator_authorization_packet_landed
admin_aas_exponential_value_pathfinder_landed
admin_aas_pre_dawn_synthesis_handoff_landed
```

These claims are internal/admin-only. They do not promote customer/public/runtime/reputation/payment/location/dispatch readiness.

## Still blocked

Do not infer any of the following from tonight's work:

```text
customer_copy_ready
customer_delivery_approved
publication_approved
public_catalog_ready
public_route_ready
public_pricing_or_customer_quote_ready
paid_pilot_ready
operator_queue_launch_ready
autonomous_dispatch_ready
live_acontext_runtime_parity
acontext_sink_ready
acontext_retrieval_ready
irc_runtime_session_manager_enhanced
autojob_integration_ready
frontier_academy_expansion_ready
kk_v2_swarm_ready
erc8004_reputation_ready
worker_skill_dna_ready
payment_or_production_reverified
exact_gps_or_raw_metadata_release_allowed
private_operator_context_release_allowed
raw_transcript_authority
worker_copyable_aas_doctrine
```

## Daytime recommendations

### 1. Best engineering move

If Docker/runtime readiness can be proven first, build or run exactly one internal/admin Acontext prerequisite/parity attempt record for:

```text
acontext_runtime_memory_prerequisites_then_single_live_parity_attempt
```

Keep it isolated from customer/public surfaces. If Docker or Acontext is still blocked, add only a prerequisite/recovery record and do not claim parity.

### 2. Best product decision move

If Saúl wants customer exposure, answer exactly one prepared authorization question with a separate human/operator artifact:

- Retail Reality selected-boundary approval/hold; or
- Compliance Desk delivery/publication gate with exact delivery path.

Without that real answer, keep both internal/admin-only.

### 3. Best coordination move

Use the four invariant IDs from the pathfinder/handoff in any daytime continuation:

```text
proof_anchor_id
coordination_session_id
compact_decision_id
review_packet_id
```

This keeps agents aligned by proof boundaries instead of broad transcript context.

### 4. Best safety move

Keep `DREAM-PRIORITIES.md` as the dream-session firewall. Do not pull AutoJob, expand Frontier Academy, work KK v2, or restart KarmaCadabra v2 from stale cron text unless Saúl updates the priority file or gives a fresh explicit daytime instruction.

## Verification before handoff

Focused verification for the new handoff:

```text
.venv/bin/python -m pytest -q mcp_server/tests/city_ops/test_aas_pre_dawn_synthesis_handoff.py
# 9 passed
```

Full city-ops verification should follow before merge/publish handoff.

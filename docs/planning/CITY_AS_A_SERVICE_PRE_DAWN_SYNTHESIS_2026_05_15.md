# City as a Service — Pre-Dawn Synthesis 2026-05-15

> Time: 05:00 America/New_York  
> Scope: Execution Market AAS / City-as-a-Service only  
> Status: night-to-day synthesis; no new product surface; no new readiness claim  
> Governing priority: `~/clawd/DREAM-PRIORITIES.md`

## 1. Priority decision

This synthesis read `~/clawd/DREAM-PRIORITIES.md` first and followed it over the stale cron payload.

Therefore this session did **not** pull, analyze, edit, or expand:

- AutoJob
- Frontier Academy
- KK v2
- KarmaCadabra v2

The active focus remains Execution Market AAS / City-as-a-Service only.

## 2. What tonight actually clarified

Tonight produced two converging tracks:

### Track A — cautious customer-exposure boundary

The three-family AAS packaging work reached the first possible customer-exposure fork, but did not cross it.

Current chain:

```text
three-family explicit hold decisions
-> packaging review packet
-> packaging/pricing/operator-workflow review board
-> single-boundary human approval request
-> approval-record schema gate
-> operator review brief
-> fail-closed approval-record validator
```

The selected boundary is deliberately tiny:

```text
Compliance Desk package label only:
"Visible posting / notice compliance snapshot"
```

The important synthesis: the system now knows exactly what a later human approval record must look like, and it knows how to reject drift. It still does **not** have that approval.

### Track B — system-integration flywheel

The AAS integration work made the cross-system operating pattern inspectable:

```text
invariant IDs
+ declared-vs-verified badges
+ sticky blocked claims
+ one next-proof slot
```

This pattern connects:

- reviewed memory / Acontext planning
- IRC/session handoff discipline
- cross-project decision support
- observability focused on boundary preservation
- payment and production-infra confidence as context, not unreverified claims

The read surface makes the pattern available to future operators and agents without requiring raw transcript replay.

## 3. Current earned safe claims

The latest earned claims are narrow:

- `aas_single_boundary_approval_record_validator_landed`
- `admin_system_integration_flywheel_surface_landed`

They mean only:

1. A future human-created approval record for the one Compliance Desk label boundary can be validated fail-closed.
2. The AAS system-integration flywheel can be inspected through an internal/admin read surface.

They do not approve customer copy, customer delivery, publication, routes, public pricing, queue launch, dispatch, reputation, live Acontext, exact GPS/raw metadata exposure, domain-authority claims, or worker-copyable doctrine.

## 4. System connections

### 4.1 AAS package ladder now has a launch-control seam

Before tonight, the AAS ladder could repeatedly say "held." Now it can also say:

```text
held -> pending exact human approval -> future record validated/rejected fail-closed
```

That is the minimum launch-control seam. It is still internal/admin, but it is precise enough for daytime to make a real decision without changing code first.

### 4.2 Acontext remains the next product-significant proof, not another planning surface

The integration flywheel keeps pointing back to one proof:

```text
one live Acontext write/retrieve parity pass
```

Only run that after prerequisites are real. Until then, do not claim live sink readiness, runtime parity, durable memory writes, or autonomous dispatch.

### 4.3 Payment and infrastructure confidence should be re-probed separately

The flywheel intentionally prevents stale production confidence from becoming a fresh claim. If daytime wants to repeat payment/network/API health claims, run a separate probe and cite current outputs. Planning artifacts should not reverify production.

## 5. Daytime priorities

### Priority 1 — Human approval decision for the one boundary

If customer exposure is desired, create one real human-operator approval record for exactly:

```text
Compliance Desk / internal_package_label_only / "Visible posting / notice compliance snapshot"
```

Then validate it with:

```text
validate_aas_single_boundary_human_operator_approval_record(...)
```

The record must preserve:

- source brief id and digest parity
- source schema-gate id and digest parity
- exact approved text equality
- non-secret human approval reference
- UTC approval timestamp
- redaction evidence references
- delivery path still `none_no_customer_delivery_authorized`
- scope `selected_internal_package_label_only`
- all future false flags false
- all still-blocked claims carried forward

Even a valid record only accepts the narrow internal label boundary. It still does not authorize customer delivery, publication, public pricing, routes, queue launch, dispatch, reputation, live runtime, exact GPS/raw metadata, domain authority, or worker doctrine.

### Priority 2 — One live Acontext parity pass, only if prerequisites are real

If Docker/Acontext/API/dashboard prerequisites are available, run one write/retrieve parity pass using the existing transport packet contract. Stop after one pass and record the result.

If prerequisites are not available, do not broaden surfaces. Continue only guardrails that preserve invariant IDs, declared-vs-verified badges, sticky blocked claims, and one next-proof slot.

### Priority 3 — Separate current production/payment probe before repeating claims

If a daytime brief needs current payment or infrastructure confidence, run a fresh probe. Do not cite older 8/8, API, payment, or network claims as if the 05:00 synthesis reverified them.

## 6. Standing blocked list

Still blocked by default:

- customer copy and customer delivery
- publication and public/catalog routes
- controlled pilot / front-door SKU
- public prices and customer quotes
- operator queue launch
- dispatch and autonomous dispatch
- ERC-8004 reputation receipts
- worker Skill DNA
- worker-copyable doctrine
- live Acontext sink readiness and runtime parity
- exact GPS/raw metadata release
- raw transcript authority
- domain-authority / legal / regulator / notarial / custody / emergency / safety / repair / insurance / SLA / official-report / fault-liability claims

## 7. Morning conclusion

The night did not make Execution Market AAS public-ready. It made the next decision safer.

Daytime now has two clean switches:

1. **Customer exposure switch:** create and validate exactly one human approval record for the Compliance Desk label boundary, or keep it held.
2. **Runtime-memory switch:** clear prerequisites and run exactly one Acontext parity pass, or keep runtime claims blocked.

Everything else should remain internal/admin proof discipline.

# City-as-a-Service — Pre-Dawn Synthesis (2026-05-21)

## Governing priority

`~/clawd/DREAM-PRIORITIES.md` was read first and controlled the session. The cron payload still referenced AutoJob, Frontier Academy, and KK v2, but the active dream priority file explicitly stops AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2.

Those tracks were not pulled, analyzed, edited, or expanded. Work stayed inside Execution Market AAS / City-as-a-Service.

## Night pattern

Tonight turned the May 20 handoff into a stronger **claim-control plane** for AAS.

The progression was deliberately conservative:

```text
cross-family approval-state matrix
→ launch-claim quarantine board
→ launch-claim quarantine read surface
→ strength-connection control packet
→ morning synthesis / daytime handoff
```

The important product shift is that AAS now has a reusable way to keep tempting product-language claims out of customer/public surfaces until a named proof artifact exists. The system can now show an operator:

- which AAS families have approval records;
- which families have delivery authorization;
- which launch claims are quarantined;
- what the smallest proof for each quarantined claim is;
- which coordination/runtime strengths are declared context versus currently verified proof.

No part of that chain creates customer copy, delivery authorization, publication readiness, public routes, pricing, queue launch, dispatch, reputation, runtime parity, payment/production reverification, exact GPS/raw metadata release, domain-authority claims, or worker-copyable doctrine.

## What connected tonight

### 1. Approval-state truth became a portfolio view

The new cross-family matrix connects three AAS families without promoting any of them:

```text
Compliance Desk: one approved text boundary, no authorized delivery path
Document / Handoff: pending approval-request read surface, no approval record
Incident Verification: future approval-record validator, no approval record
```

The key boundary is now easy to inspect:

```text
approval record != delivery authorization
approval request != approval record
validator != human approval
```

### 2. Launch-language risk became explicit quarantine

The claim quarantine board and read surface convert blocked claims into named buckets:

- customer/public exposure;
- pricing/operator launch;
- dispatch/reputation/worker Skill DNA;
- runtime/payment/production;
- GPS/raw metadata/domain-authority.

Each bucket stays marked `QUARANTINED — NOT SAFE TO CLAIM` until its exact smallest-proof requirement exists. This is the strongest anti-overclaim surface in the AAS stack so far.

### 3. Coordination strength became a bounded control packet

The strength-connection packet ties the coordination metrics read surface to the intelligence-flow compounder without turning either into runtime state. The durable pattern is:

```text
invariant IDs
+ declared-vs-verified badges
+ sticky safe/blocked claims
+ exactly one next-proof slot
= agent coordination that compounds without overclaiming
```

That pattern should be reused for every future AAS family and every future handoff.

## Current safe claims from tonight

```text
admin_aas_cross_family_approval_state_matrix_landed
admin_aas_claim_quarantine_board_landed
admin_aas_claim_quarantine_read_surface_landed
admin_aas_strength_connection_control_packet_landed
```

These claims are internal/admin-only.

## Still blocked

Do not infer any of the following from tonight's work:

```text
customer_copy_ready
customer_delivery_approved
publication_approved
public_catalog_ready
public_route_ready
controlled_pilot_ready
public_pricing_or_customer_quote_ready
operator_queue_launch_ready
autonomous_dispatch_ready
erc8004_reputation_ready
worker_skill_dna_ready
live_acontext_runtime_parity
acontext_sink_ready
payment_or_production_reverified
exact_gps_or_raw_metadata_release_allowed
raw_transcript_authority
domain_or_legal_or_regulator_or_notarial_or_custody_authority
emergency_or_safety_or_repair_or_insurance_or_sla_or_official_report_or_fault_liability_authority
worker_copyable_aas_doctrine
```

## Daytime recommendations

### 1. Best internal/admin product slice

Build an internal/admin route preflight or mount manifest for the claim quarantine read surface:

```text
mcp_server/city_ops/fixtures/aas_package_ladder/aas_claim_quarantine_read_surface.json
```

It should remain fail-closed: no network route claim until admin auth, source parity, pass-through response semantics, and no customer/public access are proven.

### 2. Best operator-learning slice

Build an operator regret / prevented-claim panel that records which quarantined claims were blocked during review.

This would make AAS safer without moving toward public launch. It should consume the quarantine read surface and write only an internal/admin prevented-claim record, not customer copy or public copy.

### 3. Best customer-exposure slice, only if desired

Choose exactly one family and exactly one named delivery path. The safest candidate remains the existing Compliance Desk approved text boundary, but its current delivery path is explicitly absent:

```text
authorized_delivery_path=none_no_customer_delivery_authorized
```

The next proof would be a separate explicit human operator decision for a named delivery path, followed by a delivery/publication gate. Do not flip readiness from the existing approval record or quarantine board.

### 4. Runtime-memory slice

Acontext remains blocked until Docker image pull/cache or trusted mirror, compose startup, localhost API/dashboard reachability, and a rebuilt empty readiness gate are real. The strength packet keeps exactly one next-proof slot for this: clear prerequisites, then attempt one live write/retrieve parity pass only if the gate is empty.

## Morning handoff

Daytime should treat May 21 as a choice between three narrow moves:

1. **Claim quarantine route preflight / mount manifest** for an internal/admin operator surface.
2. **Operator regret / prevented-claim panel** over quarantined launch claims.
3. **Acontext prerequisite repair** before any runtime-memory parity attempt.

Customer exposure should happen only through a separate named delivery-path decision, not by broadening the current approval/quarantine artifacts.

AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 remain stopped for dream work by `DREAM-PRIORITIES.md`.

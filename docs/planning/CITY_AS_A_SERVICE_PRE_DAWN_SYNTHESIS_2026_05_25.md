# City-as-a-Service — Pre-Dawn Synthesis (2026-05-25)

## Governing priority

`~/clawd/DREAM-PRIORITIES.md` was read first and controlled the session. It explicitly stops AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 during dreams.

The cron payload still carried older requests for AutoJob pull/analysis, Frontier Academy expansion, and KK v2 work. Those tracks were not pulled, analyzed, edited, or expanded. Work stayed inside Execution Market AAS / City-as-a-Service.

## Repo state

- Repo: `projects/execution-market`
- Branch: `feat/operator-route-regret-panel`
- Sync check: `git pull --ff-only` returned `Already up to date`
- Pre-existing untracked file left untouched: `scripts/sign_req.mjs`
- Latest pushed implementation commit before this synthesis: `410e2ba1` (`Add Local Data Collection operator schema gate`)

## Night pattern

Tonight moved Local Data Collection AAS from a ranked concept into a conservative internal/admin proof ladder:

```text
Local Data Collection fixture/review gate
-> synthetic local reviewed fixture
-> internal package record
-> read-only operator surface
-> customer-output schema gate
```

The important product shift is that Local Data Collection can now be treated as a method-bounded micro-proof package rather than a vague dataset promise. The lane proves one place/context, one observation window, one count-or-measurement question, method notes, uncertainty, ambiguity/occlusion, what-was-checked, and what-was-not-checked — while still refusing statistical, continuous-monitoring, exactness, certification, predictive, customer-delivery, dispatch, reputation, runtime, location/raw-metadata, and worker-doctrine claims.

No part of tonight's work creates customer copy, delivery authorization, publication readiness, public/catalog routes, dataset publication, analytics output, statistical representativeness, continuous monitoring, official dataset certification, exactness certification, predictive analytics, pricing, queue launch, dispatch, ERC-8004 reputation, worker Skill DNA, live Acontext/runtime parity, exact GPS/raw metadata release, or worker-copyable data-collection doctrine.

## What connected tonight

### 1. Local Data Collection is the reusable micro-dataset ladder

Retail Reality proved the approval-boundary pattern. Local Data Collection now proves the adjacent low-authority data-collection pattern:

```text
bounded method
+ explicit uncertainty
+ ambiguity / occlusion notes
+ reviewed output fields
+ pass-through operator surface
+ schema-only customer-output boundary
```

This is useful because many agent tasks eventually need local observations, but almost none should be allowed to become a dataset, trend, monitoring product, exactness certificate, or public route by inference.

### 2. The schema gate is a future-output boundary, not output

The latest Local Data Collection artifact only defines allowed and forbidden future fields. It intentionally does not create a sample, customer copy, delivery path, publication approval, dataset, analytics, dispatch instruction, reputation event, or runtime write.

Safe claims now include:

```text
local_data_collection_fixture_review_gate_landed
local_data_collection_local_reviewed_fixture_landed
local_data_collection_internal_package_record_landed
local_data_collection_operator_read_surface_landed
local_data_collection_customer_output_schema_gate_landed
```

The next proof must stay separate: one internal/admin sample output against the schema, followed by an explicit hold/approval decision. Default remains hold.

### 3. Blocked claims are the product contract

The strongest recurring pattern across CaaS/AAS is now clear:

```text
proof packaging is only trustworthy when blocked claims travel beside safe claims
```

For Local Data Collection, the blocked claims are not bureaucratic noise. They are what keep a useful observation from accidentally becoming a customer dataset, a continuous monitor, a certified measurement, a public catalog offer, or worker-copyable doctrine.

### 4. Acontext and reputation remain downstream gates

Local Data Collection will eventually be valuable for memory, worker matching, and ERC-8004 reputation, but not yet. The current artifacts only prove local packaging discipline. Runtime-memory parity still depends on Docker/Acontext prerequisites, and reputation still requires a separate event-source/signer/privacy/rollback design.

## Current safe claims from tonight

```text
local_data_collection_fixture_review_gate_landed
local_data_collection_local_reviewed_fixture_landed
local_data_collection_internal_package_record_landed
local_data_collection_operator_read_surface_landed
local_data_collection_customer_output_schema_gate_landed
```

These claims are internal/admin-only or schema-gate-only. They do not promote customer/public/runtime/reputation/payment/location/data-certification readiness.

## Still blocked

Do not infer any of the following from tonight's work:

```text
customer_copy_ready
customer_delivery_approved
publication_approved
public_catalog_ready
public_route_ready
controlled_pilot_ready
front_door_sku_ready
public_pricing_or_customer_quote_ready
operator_queue_launch_ready
autonomous_dispatch_ready
dataset_publication_ready
analytics_ready
statistical_representativeness
continuous_monitoring
official_dataset_certification
exactness_beyond_observed_method
predictive_analytics
erc8004_reputation_ready
worker_skill_dna_ready
live_acontext_runtime_parity
acontext_sink_ready
payment_or_production_reverified
exact_gps_or_raw_metadata_release_allowed
private_operator_context_release_allowed
raw_transcript_authority
worker_copyable_data_collection_doctrine
```

## Daytime recommendations

### 1. Best product move

Create one synthetic internal/admin Local Data Collection sample output against `local_data_collection_customer_output_schema_gate.json`.

Required constraints:

- consume only the schema gate;
- populate only the allowed customer-output fields;
- remain synthetic, internal/admin, and non-site-specific;
- include method, uncertainty, ambiguity/occlusion, what-was-checked, what-was-not-checked, privacy redaction, and limitations/non-guarantee notices;
- keep every customer/public/catalog/dataset/analytics/pricing/dispatch/reputation/runtime/location/exactness/representativeness/monitoring/certification/prediction/worker-doctrine flag false.

### 2. Required follow-up gate

After the sample output, create a separate explicit hold/approval decision over that exact sample. Default to hold.

The decision artifact should name the sample digest, selected text boundary, redaction checks, delivery-path status, and blocked claims. Do not mutate the sample into approval.

### 3. Best synthesis move

Compare Retail Reality and Local Data Collection as two reusable AAS templates:

```text
Retail Reality = approval-boundary template for observed storefront state
Local Data Collection = method-bounded micro-observation template
```

A small cross-family matrix could help daytime decide which template becomes the first customer-facing experiment, but only after a human approval artifact exists.

### 4. Best runtime move

Acontext remains a separate prerequisite track. Do not attach runtime-memory, worker Skill DNA, or ERC-8004 reputation claims to Local Data Collection until Docker/Acontext prerequisites are repaired and exactly one live write/retrieve parity pass is explicitly allowed by a fresh gate.

## Verification before handoff

Final checks after this doc-only synthesis:

```text
git diff --check
.venv/bin/python -m pytest -q mcp_server/tests/city_ops
1316 passed
```

## Morning handoff

Daytime should choose exactly one narrow move:

1. **Local sample output:** build one internal/admin sample output against the Local Data Collection schema gate, then hold.
2. **Hold decision:** if the sample already exists, create the separate explicit hold/approval decision over the exact sample digest.
3. **Cross-family matrix:** compare Retail Reality and Local Data Collection approval/method boundaries without adding customer exposure.
4. **Runtime prerequisite repair:** fix Docker/Acontext before any live memory/runtime claims.

AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 remain stopped for dream work by `DREAM-PRIORITIES.md`.

# City-as-a-Service — Pre-Dawn Synthesis (2026-05-31)

> Scope: Execution Market AAS / City-as-a-Service only.
> Status: 5 AM synthesis and daytime handoff; no customer/public/runtime approval.
> Governing priority: `~/clawd/DREAM-PRIORITIES.md`.

## 1. Priority firewall

`~/clawd/DREAM-PRIORITIES.md` was read first. It explicitly allows Execution Market AAS / City-as-a-Service work and explicitly stops AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2.

The stale cron payload again listed AutoJob, Frontier Academy, and KK v2 work. Those instructions were intentionally skipped. No stopped-project files were pulled, analyzed, edited, expanded, tested, or committed.

## 2. Tonight's connected stack

The May 31 night turned Acontext from “local write/retrieve worked once” into a conservative, replayable runtime-memory promotion ladder:

```text
root-prefixed local write/retrieve parity
-> internal IRC session adapter contract
-> redacted internal IRC adapter runner fixture
-> fail-closed runtime-memory promotion gate
-> disabled-by-default opt-in runtime adapter seam contract
-> cleanup/quarantine harness gate
-> multi-fixture replay gate
-> explicit operator activation decision request
```

The important synthesis: the product value is not just Acontext memory. The value is the proof discipline around memory: every candidate memory item must survive source digesting, redaction, cleanup/quarantine, replay, and explicit operator activation before it can influence runtime behavior.

## 3. What changed tonight

### 00:00 — redacted internal IRC adapter runner fixture

A bounded local Acontext runner proved one sanitized IRC-shaped memory write/retrieve flow, while persisting only status codes and safety booleans. It did not persist bearer values, project secrets, session IDs, message IDs, raw logs, private context, GPS, or raw metadata.

Safe claim: `admin_acontext_internal_irc_session_adapter_runner_fixture_landed`.

### 01:00 — runtime-memory promotion gate

The runner success was converted into a fail-closed promotion gate so one local success could not become a broad runtime, customer, dispatch, reputation, payment, GPS, private-context, or worker-doctrine claim.

Safe claim: `admin_acontext_runtime_memory_promotion_gate_landed`.

### 02:00 — opt-in runtime adapter seam contract

The next insertion point was defined as `irc_session_manager_memory_sink`, but explicitly disabled, design-only, and blocked behind cleanup/quarantine, multi-fixture replay, rollback, and operator opt-in requirements.

Safe claim: `admin_acontext_opt_in_runtime_adapter_seam_contract_landed`.

### 03:00 — cleanup/quarantine harness gate

A deterministic local harness covered success cleanup and failed-write quarantine paths while persisting only labels, status classes, and booleans.

Safe claim: `admin_acontext_cleanup_quarantine_harness_gate_landed`.

### 04:00 — multi-fixture replay gate

The harness expanded into replay over three reviewed sanitized local cases: success/retrieve/cleanup, failed-write/quarantine, and schema-mismatch hold. Runtime handles and raw payloads stayed non-persisted.

Safe claim: `admin_acontext_multi_fixture_replay_gate_landed`.

### 05:00 — operator activation decision request

The pre-dawn pass drafted the separate human/operator request that must exist before any runtime adapter registration or IRC/session-manager mutation:

- `CITY_AS_A_SERVICE_ACONTEXT_OPERATOR_ACTIVATION_DECISION_REQUEST_2026_05_31.md`

This request records no approval. Default remains hold.

## 4. Strategic connections

### Memory system ↔ Acontext

Acontext can become durable agent memory only if it does not become an uncontrolled raw transcript sink. Tonight's ladder establishes the safer pattern:

```text
sanitized candidate -> source digest -> local/replay proof -> operator activation decision -> bounded runtime mutation
```

That pattern is reusable for future Execution Market AAS evidence, IRC coordination notes, and cross-session handoffs.

### IRC/session coordination ↔ proof discipline

IRC-style coordination should not rely on raw message replay. The useful transferable unit is a compact proof packet: invariant IDs, source digest, safe claims, blocked claims, and next proof slot.

### Cross-project intelligence ↔ priority firewall

The night repeatedly protected the current AAS priority from stale cron context. This matters operationally: cross-project awareness should rank and filter work, not reopen stopped projects by accident.

### AAS portfolio ↔ runtime truth

The City-as-a-Service portfolio now has two clean daytime forks:

1. **Runtime-memory fork:** decide whether to continue toward disabled design-only adapter wiring.
2. **Product-exposure fork:** separately choose one human/operator approval boundary for Retail Reality or Compliance Desk.

They should not be merged. Runtime memory does not authorize customer exposure, and customer exposure does not prove runtime parity.

## 5. Daytime recommendations

1. **Default hold:** keep Acontext runtime mutation blocked unless Saúl explicitly chooses a runtime-memory path today.
2. **If continuing runtime memory:** approve only disabled design-only wiring first, behind a kill switch. Do not run a live IRC/session mutation from the replay gate.
3. **If product exposure is the priority:** ignore runtime memory and choose exactly one held AAS boundary for human review: Retail Reality selected boundary or Compliance Desk delivery/publication gate.
4. **If neither decision is available:** stop at the current replay + decision-request layer and use the docs as the daytime coordination entrypoint.

## 6. Still blocked

The May 31 stack does **not** approve:

```text
runtime adapter registration or enablement
IRC session-manager mutation
cross-project autorouting
customer copy/customer delivery/publication
public catalog routes
public pricing or customer quotes
operator queue launch
worker dispatch/autonomous dispatch
ERC-8004 reputation
Worker Skill DNA
payment or production readiness
exact GPS/raw metadata release
private operator context release
domain/legal/emergency/safety/repair/insurance authority claims
worker-copyable doctrine
general Acontext sink readiness
unbounded runtime parity
```

## 7. Daytime entrypoints

- Runtime activation decision: `CITY_AS_A_SERVICE_ACONTEXT_OPERATOR_ACTIVATION_DECISION_REQUEST_2026_05_31.md`
- Latest replay proof: `CITY_AS_A_SERVICE_4AM_HANDOFF_2026_05_31.md`
- Runtime seam contract: `CITY_AS_A_SERVICE_ACONTEXT_OPT_IN_RUNTIME_ADAPTER_SEAM_CONTRACT_IMPLEMENTATION.md`
- Cleanup/quarantine gate: `CITY_AS_A_SERVICE_ACONTEXT_CLEANUP_QUARANTINE_HARNESS_GATE_IMPLEMENTATION.md`
- Board to update for operators: `CITY_AS_A_SERVICE_DAYTIME_EXECUTION_BOARD.md`

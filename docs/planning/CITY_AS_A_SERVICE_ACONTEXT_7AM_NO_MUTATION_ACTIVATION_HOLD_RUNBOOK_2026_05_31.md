# City-as-a-Service — Acontext 7 AM No-Mutation Activation/Hold Runbook (2026-05-31)

> Scope: Execution Market AAS / City-as-a-Service internal/admin coordination only.
> Status: daytime operator checkpoint; default is `hold_no_runtime_mutation`.
> This is a documentation/runbook artifact only. It adds no implementation claim and records no approval.

## 1. Governing firewall

`~/clawd/DREAM-PRIORITIES.md` remains the governing dream/daytime handoff priority for this checkpoint:

- Allowed focus: Execution Market AAS / City-as-a-Service.
- Stopped tracks: AutoJob, Frontier Academy, KK v2, KarmaCadabra v2.
- Do not use this Acontext checkpoint to reopen, pull, analyze, edit, test, or route any stopped project.

## 2. Source artifacts to trust for this checkpoint

| Artifact | Role | What it proves | What it does **not** prove |
|---|---|---|---|
| `CITY_AS_A_SERVICE_6AM_FINAL_WRAP_2026_05_31.md` | sealed morning coordination marker | the May 31 Acontext proof ladder ended at final wrap | no runtime approval, customer exposure, dispatch, reputation, payment, GPS/raw metadata, private-context, or worker-doctrine readiness |
| `CITY_AS_A_SERVICE_ACONTEXT_OPERATOR_ACTIVATION_DECISION_REQUEST_2026_05_31.md` | operator decision request | the exact human decision needed before mutation | not an approval record |
| `mcp_server/city_ops/fixtures/proof_blocks/redirect_outdated_packet_001/acontext_multi_fixture_replay_gate.json` | latest replay source | three reviewed sanitized local fixtures replayed through success, quarantine, and hold paths | no live IRC/session-manager mutation and no general Acontext sink readiness |
| `CITY_AS_A_SERVICE_PRE_DAWN_SYNTHESIS_2026_05_31.md` | daytime coordination entrypoint | separates runtime-memory and product-exposure forks | no merged runtime/product/customer readiness |

Reference digest for the replay source at this checkpoint:

```text
acontext_multi_fixture_replay_gate.json
sha256:bc45e1808e29360bcced6c106b48ec24c94fa8aeb91c35dfcf7aba63dd3b057f
```

## 3. One-question daytime decision

Before any code path, runtime wiring, or local activation work, the operator must answer exactly one question:

```text
For candidate irc_session_manager_memory_sink, do we stay in hold, approve disabled design-only wiring, or approve one bounded local activation test?
```

Permitted answers:

1. `hold_no_runtime_mutation` — keep adapter unregistered; continue using only docs/local/replay artifacts.
2. `approve_design_only_wiring_default_off` — allow a future implementation to add only a disabled registration path behind a kill switch; no live IRC/session mutation.
3. `approve_one_bounded_local_activation_test` — allow exactly one sanitized local candidate through the adapter with cleanup/quarantine and immediate rollback.

If the answer is absent, implied, ambiguous, or mixed with product/customer goals, treat it as:

```text
hold_no_runtime_mutation
```

## 4. Default hold runbook

Use this path unless Saúl explicitly chooses option 2 or 3.

1. Leave `irc_session_manager_memory_sink` unregistered and disabled.
2. Do not mutate IRC/session-manager runtime code.
3. Do not start Compose or live Acontext services for this checkpoint.
4. Do not create customer/public/pricing/dispatch/reputation/payment claims from replay evidence.
5. Keep the current source docs as the daytime handoff:
   - `CITY_AS_A_SERVICE_6AM_FINAL_WRAP_2026_05_31.md`
   - `CITY_AS_A_SERVICE_ACONTEXT_OPERATOR_ACTIVATION_DECISION_REQUEST_2026_05_31.md`
   - this 7 AM runbook.
6. If asked for a next action without an explicit operator answer, say: “Current default is hold; choose one runtime-memory option or one separate product-exposure boundary.”

## 5. If design-only wiring is explicitly approved later

A separate approval record must exist first. Then the implementation may do only this:

- Add a disabled registration path for `irc_session_manager_memory_sink`.
- Keep the kill switch default-off.
- Persist no bearer values, project secrets, session IDs, message IDs, raw metadata, exact GPS, private context, customer copy, or worker instructions.
- Preserve cleanup/quarantine and rollback requirements.
- Add focused tests proving disabled/default-off behavior.

Stop line: design-only wiring still does not authorize a live local activation test or live IRC/session-manager mutation.

## 6. If one bounded local activation test is explicitly approved later

A separate approval record must name all required fields from the decision request before any test begins:

- source artifact ID and digest,
- exact selected option,
- non-secret human/operator reference,
- UTC timestamp,
- kill-switch name and default-off confirmation,
- rollback instruction,
- cleanup/quarantine requirement,
- sanitized candidate source fixture ID,
- carried-forward blocked claims.

The test must remain local, sanitized, single-candidate, cleanup/quarantine-gated, and immediately rolled back. It must not become live IRC/session-manager behavior by default.

## 7. Product-exposure fork stays separate

Runtime-memory approval is not customer exposure. If the daytime priority is product exposure, ignore runtime activation and choose exactly one separate AAS boundary:

- Retail Reality selected-boundary approval/hold, or
- Compliance Desk delivery/publication gate with an exact delivery path.

No Retail Reality or Compliance Desk approval can be inferred from this runbook.

## 8. Blocked claims preserved

This checkpoint preserves the following blocked claims unchanged:

```text
runtime adapter registration
runtime adapter enablement
IRC session-manager mutation
cross-project autorouting
customer copy/customer delivery/publication
public catalog route
pricing or customer quote
operator queue launch
worker dispatch/autonomous dispatch
ERC-8004 reputation
Worker Skill DNA
payment or production readiness
exact GPS/raw metadata release
private operator context release
domain/legal/emergency/safety/repair/insurance authority claims
worker-copyable doctrine
general Acontext sink readiness
unbounded runtime parity
AutoJob integration
Frontier Academy expansion
KK v2 swarm readiness
KarmaCadabra v2 work
```

## 9. Safe claim

No new implementation claim is added by this file.

Safe coordination statement only:

```text
7 AM checkpoint preserved hold_no_runtime_mutation and made the next human decision explicit.
```

# City-as-a-Service — Pre-Dawn Synthesis (2026-05-23)

## Governing priority

`~/clawd/DREAM-PRIORITIES.md` was read first and controlled the session. It explicitly stops AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 during dreams.

The stale cron payload requested AutoJob pull/analysis, Frontier Academy expansion, and KK v2 work. Those tracks were not pulled, analyzed, edited, or expanded. Work stayed inside Execution Market AAS / City-as-a-Service.

## Night pattern

Tonight connected three AAS lanes into one daytime operating picture:

```text
prevented-claim route proof -> compact route handoff packet
Retail Reality fixture gate -> local reviewed fixture -> internal package record
Acontext prerequisite probe -> daemon-down recheck artifact
```

The synthesis is conservative: Execution Market AAS is strongest when every tempting launch claim becomes either a reviewed proof slot or an explicit blocker, never an implied readiness story.

No part of tonight's work creates customer copy, delivery authorization, publication readiness, public routes, pricing, queue launch, dispatch, ERC-8004 reputation, worker Skill DNA, runtime parity, payment/production reverification, exact GPS/raw metadata release, domain/legal authority, raw transcript authority, or worker-copyable doctrine.

## What connected tonight

### 1. Claim quarantine became stable pickup state

The prevented-claim trend route preflight already proved a guarded internal/admin route shape. The new handoff packet over that preflight makes the route proof easy to pick up without accidentally expanding it.

Important boundary:

```text
route proof != launch readiness
handoff packet != delivery approval
internal/admin pass-through != customer/public surface
```

The packet sets `route_expansion_paused=true`, which is the correct product posture unless a human operator explicitly asks for another internal read need.

### 2. Retail Reality became the low-authority AAS proving ground

Retail Reality advanced from fixture/review gate to local reviewed fixture and internal package record. Its value is not the retail category by itself; its value is the low-authority proof grammar:

```text
one storefront-style observation
+ one observation window
+ one availability/hours question
+ source-bounded review
+ blocked authority classes beside safe claims
```

That grammar is reusable for other lightweight AAS families because it avoids heavy authority traps. Observed state is not permanent truth. Availability is not an inventory guarantee. Packaging is not publication. Internal proof is not dispatch.

### 3. Runtime parity stayed honestly blocked

The Acontext daemon recheck updated the blocker from older image-pull uncertainty to the current first prerequisite: Docker API/socket unavailable, Buildx unhealthy, image/container inventory unavailable, and local Acontext API/dashboard unreachable.

That is useful because it stops future sessions from chasing later proof steps before the first runtime prerequisite is real.

Correct sequence remains:

```text
restore Docker daemon/socket
-> recheck required image inventory
-> complete trusted pull/cache path
-> start Compose
-> verify API/dashboard health
-> rebuild empty readiness gate
-> attempt exactly one live write/retrieve parity pass only if the gate allows it
```

## Current safe claims from tonight

```text
internal_admin_aas_claim_quarantine_prevented_claim_trend_route_handoff_packet_landed
retail_reality_fixture_review_gate_landed
retail_reality_local_reviewed_fixture_landed
retail_reality_internal_package_record_landed
admin_acontext_runtime_memory_daemon_recheck_landed
```

These claims are internal/admin-only or local reviewed-fixture/package claims. They do not promote customer/public/runtime/reputation/payment/location/legal readiness.

## Still blocked

Do not infer any of the following from tonight's work:

```text
customer_copy_ready
customer_delivery_approved
publication_approved
public_catalog_ready
public_route_ready
controlled_pilot_ready
public_pricing_or_customer_quote_ready
operator_queue_launch_ready
autonomous_dispatch_ready
erc8004_reputation_ready
worker_skill_dna_ready
live_acontext_runtime_parity
acontext_sink_ready
payment_or_production_reverified
exact_gps_or_raw_metadata_release_allowed
raw_transcript_authority
domain_or_legal_or_regulator_or_notarial_or_custody_authority
emergency_or_safety_or_repair_or_insurance_or_sla_or_official_report_or_fault_liability_authority
permanent_business_status_claim
inventory_guarantee
brand_compliance_certification
employee_performance_judgment
consumer_safety_claim
worker_copyable_aas_doctrine
```

## Daytime recommendations

### 1. Best product move

Create a read-only Retail Reality operator coverage/read surface over:

```text
mcp_server/city_ops/fixtures/aas_package_ladder/retail_reality_internal_package_record.json
```

Required gate:

- consume only the package record;
- preserve source artifact IDs and digests;
- render pass-through state, not interpretation;
- keep `safe_to_claim[]` beside `do_not_claim_yet[]`;
- keep customer/public/pricing/dispatch/reputation/runtime/location/permanent-status/inventory/compliance/safety/worker-doctrine readiness false;
- do not register a public route or create customer copy.

### 2. Best runtime move

Repair Docker/Acontext prerequisites before any more Acontext parity artifacts. The daemon-down recheck is now the daytime source of truth for the first blocker.

### 3. Best operator-learning move

Do not expand claim-quarantine routes by default. The trend-route handoff packet already says enough for pickup. If another operator surface is needed, require a fresh internal/admin read need plus a fail-closed mount manifest.

### 4. Best strategic posture

Treat Retail Reality as the prototype for low-authority AAS packaging and Acontext as the runtime-memory blocker. One lane can advance local proof surfaces; the other must wait for infrastructure repair. Do not mix them into a premature launch story.

## Morning handoff

Daytime should choose exactly one narrow move:

1. **Retail proof surface:** build the read-only Retail Reality operator surface over the internal package record.
2. **Runtime parity:** restore Docker/Acontext prerequisites, then rerun the gate before any parity pass.
3. **Customer exposure:** only with an explicit human selected-boundary approval artifact naming exact text, redactions, delivery path, and still-blocked claims.

AutoJob, Frontier Academy, KK v2, and KarmaCadabra v2 remain stopped for dream work by `DREAM-PRIORITIES.md`.

# Execution Market MCP Server Dependencies
# Python 3.11+

# ===================
# MCP Server (Streamable HTTP Transport)
# ===================
mcp>=1.20.0  # Required for streamable HTTP transport
pydantic>=2.5.0
# starlette pinned by fastapi — do not specify separately

# ===================
# Web Framework
# ===================
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
uvloop>=0.19.0; sys_platform != 'win32'
httptools>=0.6.0

# ===================
# Database
# ===================
supabase>=2.0.0
asyncpg>=0.29.0

# ===================
# HTTP Client
# ===================
httpx>=0.26.0
aiohttp>=3.9.0
# Retry policy for facilitator HTTP calls (Phase 2 SAAS_PRODUCTION_HARDENING).
# Wraps payment + reputation calls with exponential backoff on transient
# transport errors / 5xx. Also pulled transitively via apscheduler.
tenacity>=8.2.0

# ===================
# Async
# ===================
anyio>=4.2.0

# ===================
# Blockchain / x402 / ERC-8004
# ===================
web3>=6.15.0
eth-account>=0.11.0

# ===================
# World ID 4.0 (RP Signing)
# ===================
coincurve>=20.0.0
pycryptodome>=3.20.0

# ===================
# x402 SDK (Official Ultravioleta DAO SDK)
# ===================
uvd-x402-sdk[fastapi,wallet]>=0.21.0

# ===================
# Image Processing (Verification)
# ===================
Pillow>=10.2.0
imagehash>=4.3.0
piexif>=1.1.3

# ===================
# File Type Validation (Magika — Content-Based Detection)
# ===================
magika>=0.5.1

# ===================
# AI (Multi-Provider Evidence Verification)
# ===================
anthropic>=0.30.0
openai>=1.0.0
boto3>=1.34.0
google-generativeai>=0.8.0  # Used by verification/providers.py (Gemini)

# ===================
# WebSocket Support
# ===================
websockets>=11,<16  # realtime 2.28.3 requires <16

# ===================
# Redis (Caching & Rate Limiting)
# ===================
redis>=5.0.0
hiredis>=2.3.0

# ===================
# Auth (JWT verification — HS256 + ES256 via JWKS)
# ===================
PyJWT[crypto]>=2.8.0

# ===================
# Utils
# ===================
python-dotenv>=1.0.0
python-dateutil>=2.8.0
# ===================
# Scheduling
# ===================
apscheduler>=3.10.0

# ===================
# Observability — Sentry (Phase 1.5 SAAS_PRODUCTION_HARDENING)
# ===================
# Init happens in main.py only when SENTRY_DSN env var is set (graceful
# degradation in dev/test). Extras wire the FastAPI + httpx integrations.
sentry-sdk[fastapi,httpx]>=2.0.0

# ===================
# Observability — Structured JSON logs (Phase 2.3 SAAS_PRODUCTION_HARDENING)
# ===================
# Configured in mcp_server/logging_config.py, wired from main.py after the
# Sentry block. Enables CloudWatch Insights queries by request_id, agent_id,
# task_id. Set EM_JSON_LOGS=false to fall back to text formatter locally.
python-json-logger>=2.0.7

# ===================
# Observability — Prometheus metrics (Phase 6.1 SAAS_PRODUCTION_HARDENING)
# ===================
# RED metrics via PrometheusMiddleware + business counters/gauges. Exposed
# at /metrics (admin-key protected). Graceful no-op if the package is
# absent so CI/local envs without the dep still boot.
prometheus-client>=0.19.0

# ===================
# Testing (dev)
# ===================
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

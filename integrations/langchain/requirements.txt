# Execution Market LangChain Integration Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
langchain-core>=0.1.0
httpx>=0.25.0
pydantic>=2.0.0

# Optional but recommended
langchain>=0.1.0        # For agents and chains
openai>=1.0.0          # If using OpenAI models
anthropic>=0.20.0      # If using Anthropic models

# Development dependencies (for contributors)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.0.0
mypy>=1.5.0